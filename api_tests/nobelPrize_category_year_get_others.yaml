# Test generated by RoostGPT for test nobelmaster-data using AI Type Open AI and AI Model gpt-4

# RoostTestHash=4aed6d93a0

config:
  target: '{{ $processEnvironment.API_HOST }}'
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: ./nobelPrize_category_year_get.csv
    fields:
      - category
      - year
    skipHeader: true
scenarios:
  - name: API request with validation failure
    flow:
      - get:
          url: /nobelPrize/invalidCategory/invalidYear
          expect:
            - statusCode: 400
            - hasProperty: code
            - hasProperty: message
  - name: API request with edge case
    flow:
      - get:
          url: /nobelPrize/che/12345678901234567890123456789012345678901234567890
          expect:
            - statusCode: 400
            - hasProperty: code
            - hasProperty: message
  - name: API request with not found error
    flow:
      - get:
          url: /nobelPrize/che/1900
          expect:
            - statusCode: 404
            - hasProperty: code
            - hasProperty: message
  - name: API request with unprocessable entity error
    flow:
      - get:
          url: /nobelPrize/che/abc
          expect:
            - statusCode: 422
            - hasProperty: code
            - hasProperty: message
