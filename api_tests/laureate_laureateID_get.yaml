# Test generated by RoostGPT for test nobelmaster-azureopenai using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=7d12c82059

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/laureate/1"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/laureate/string"
          expect:
            - statusCode: 400
            - hasProperty: ["code", "message"]

  - name: "API request with edge case"
    flow:
      - get:
          url: "/laureate/99999999999999"
          expect:
            - statusCode: 422
            - hasProperty: ["code", "message"]

  - name: "API request with nonexistent laureateID"
    flow:
      - get:
          url: "/laureate/0"
          expect:
            - statusCode: 404
            - hasProperty: ["code", "message"]
