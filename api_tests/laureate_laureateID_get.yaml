# Test generated by RoostGPT for test nobelmaster-data using AI Type Open AI and AI Model gpt-4

# RoostTestHash=94990962bb

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "laureate_laureateID_get.csv"
    fields:
      - "laureateID"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/laureate/{{ laureateID }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/laureate/invalidID"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/laureate/abcdefghijklmnopqrstuvwxyz1234567890abcdefghijklmnopqrstuvwxyz10"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 422
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with not found"
    flow:
      - get:
          url: "/laureate/99999999999999999999999999999999999999999999999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request without authorization"
    flow:
      - get:
          url: "/laureate/{{ laureateID }}"
          expect:
            - statusCode: 401
            - hasProperty: "code"
            - hasProperty: "message"
