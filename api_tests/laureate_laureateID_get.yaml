# Test generated by RoostGPT for test nobelmaster-data using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7d12c82059

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/laureate/1"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - hasProperty: "statuscode"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/laureate/abc"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/laureate/9999999999"
          expect:
            - statusCode: 422
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with resource not found"
    flow:
      - get:
          url: "/laureate/0"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"
