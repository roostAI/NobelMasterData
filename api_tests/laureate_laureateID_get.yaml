# Test generated by RoostGPT for test nobelPrizeData using AI Type Open AI and AI Model gpt-4

# RoostTestHash=94990962bb

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/laureate/{{ laureateID }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/laureate/invalidID"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/laureate/99999999999999999999999999"
          expect:
            - statusCode: 422

  - name: "API request with non-existent laureateID"
    flow:
      - get:
          url: "/laureate/0"
          expect:
            - statusCode: 404

  - name: "API request with negative laureateID"
    flow:
      - get:
          url: "/laureate/-1"
          expect:
            - statusCode: 422

  - name: "API request with string laureateID"
    flow:
      - get:
          url: "/laureate/stringID"
          expect:
            - statusCode: 422
