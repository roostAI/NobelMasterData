# Test generated by RoostGPT for test nobelmaster-azureopenai using AI Type Azure Open AI and AI Model roost-gpt4-32k

# RoostTestHash=5c08a4427f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 10
  payload:
    path: "nobelPrize_category_year_get.csv"
    fields: 
      - "category"
      - "year"
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/nobelPrize/{{ category }}/{{ year }}"
          capture:
            json: "$.nobelPrize"
            as: "nobelPrize"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrize"

  - name: "API request with invalid URL"
    flow:
      - get:
          url: "/invalidURL/{{ category }}/{{ year }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/nobelPrize/UnknownCategory/9999"
          expect:
            - statusCode: 422
            - hasProperty: 'code'
            - hasProperty: 'message'
