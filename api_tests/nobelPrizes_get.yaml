# Test generated by RoostGPT for test nobelmaster-data using AI Type Open AI and AI Model gpt-4

# Test generated by RoostGPT for test nobelmaster-data using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 5
  payload:
    path: "nobelPrizes_get.csv"
    fields: ["offset", "limit", "sort", "nobelPrizeYear", "yearTo", "nobelPrizeCategory", "format", "csvLang"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/nobelPrizes"
          qs:
            offset: "{{ offset }}"
            limit: "{{ limit }}"
            sort: "{{ sort }}"
            nobelPrizeYear: "{{ nobelPrizeYear }}"
            yearTo: "{{ yearTo }}"
            nobelPrizeCategory: "{{ nobelPrizeCategory }}"
            format: "{{ format }}"
            csvLang: "{{ csvLang }}"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
            - hasProperty: "meta"
            - hasProperty: "links"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidURL"
          qs:
            offset: "{{ offset }}"
            limit: "{{ limit }}"
            sort: "{{ sort }}"
            nobelPrizeYear: "{{ nobelPrizeYear }}"
            yearTo: "{{ yearTo }}"
            nobelPrizeCategory: "{{ nobelPrizeCategory }}"
            format: "{{ format }}"
            csvLang: "{{ csvLang }}"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/nobelPrizes"
          qs:
            offset: "10000000000"
            limit: "10000000000"
            sort: "!@#$%^&*()"
            nobelPrizeYear: "10000000000"
            yearTo: "10000000000"
            nobelPrizeCategory: "!@#$%^&*()"
            format: "!@#$%^&*()"
            csvLang: "!@#$%^&*()"
          expect:
            - statusCode: 422
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with resource not found"
    flow:
      - get:
          url: "/nobelPrizesNotFound"
          qs:
            offset: "{{ offset }}"
            limit: "{{ limit }}"
            sort: "{{ sort }}"
            nobelPrizeYear: "{{ nobelPrizeYear }}"
            yearTo: "{{ yearTo }}"
            nobelPrizeCategory: "{{ nobelPrizeCategory }}"
            format: "{{ format }}"
            csvLang: "{{ csvLang }}"
          expect:
            - statusCode: 404
            - hasProperty: "code"
            - hasProperty: "message"
